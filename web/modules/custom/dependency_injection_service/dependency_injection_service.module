<?php

/**
 * Implements hook_theme().
 */

function dependency_injection_service_theme($existing, $type, $theme, $path)
{
  return [
    'my-template' => [
      'variables' => [
        'data' => [],
        'title' => ''
      ],
    ],
  ];
}
