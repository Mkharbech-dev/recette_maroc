{#
/**
 * @file
 * ceps's theme implementation to display front page.
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template.
 *
 */
#}
{% include '@ceps/template-parts/header.html.twig' %}
{% if slider_show %}
  {% include '@ceps/template-parts/slider.html.twig' %}
{% endif %}
<div id="main-wrapper" class="main-wrapper">
  <div class="container">
    {% if front_sidebar %}
      <div class="main-container">
    {% endif %}
    <main id="front-main" class="homepage-content page-content" role="main">
      <a id="main-content" tabindex="-1"></a>{# link is in html.html.twig #}
      {% include '@ceps/template-parts/content_top.html.twig' %}
      {% if page.content %}
				{{ page.content }}
			{% endif %}
			{% if page.content_home %}
				<div class="homepage-content">
					{{ page.content_home }}
				</div><!--/.homepage-content -->
			{% endif %}
      {% include '@ceps/template-parts/content_bottom.html.twig' %}
    </main>
    {% if front_sidebar %}
      {% include '@ceps/template-parts/left_sidebar.html.twig' %}
      {% include '@ceps/template-parts/right_sidebar.html.twig' %}
      </div> {# /.main-container #}
    {% endif %}
  </div> <!--/.container -->
</div><!-- /main-wrapper -->
{% include '@ceps/template-parts/footer.html.twig' %}
